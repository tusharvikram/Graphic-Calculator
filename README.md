# Graphing Calc (Python)

One-liner: plots a function, lets you pick a point, shows the derivative and f(x).

## Quick start
```bash
python -m venv venv
pip install -r requirements.txt
python src/graphing.py
